@model LMSweb.ViewModel.LearnBViewModel
@{
    ViewBag.Title = "Chat";
}
@*<body class="sticky">
        <div class="container">
            <input type="text" id="message" />
            <input type="button" id="sendmessage" value="Send" />
            <input type="hidden" id="displayname" />
            <ul id="discussion">
            </ul>
        </div>
    </body>*@
<h2>Chat</h2>
<div class="container">
    <div style="width:500px; height:500px; border:2px solid #eee; border-radius:10px;">
        <div id="scroll" style="width:496px; height:420px; overflow-x:hidden; overflow-y:auto;">
            <ul id="messages" style="margin:0; padding:10px; list-style-type:none; width:100%;"></ul>
        </div>
        <div style="width:496px; height:80px; border-top:2px solid #eee;">
            <div class="row align-items-end">
                <div class="col-md-9">
                    <input type="text" id="message" placeholder="想說的話" style="width:400px; height:74px; border:0; border-bottom-left-radius:10px; resize:none;" />
                    @*<textarea id="message" placeholder="想說的話" style="width:400px; height:74px; border:0; border-bottom-left-radius:10px; resize:none;"></textarea>*@
                </div>
                <div class="col-auto">
                    <input type="button" value="Send" class="btn btn-primary" style="width:106px; height:74px;border:0; border-bottom-right-radius:10px; resize:none;" />
                    <input type="hidden" id="displayname" />
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts {
    <script src="~/Scripts/jquery.signalR-2.4.2.js"></script>
    <script src="~/Scripts/jquery.signalR-2.4.2.min.js"></script>
    <script src="~/signalr/hubs"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            var chat = $.connection.chatHub;
            $('#displayname').val(prompt('請輸入暱稱:'));
            var today = new Date();

            var currentDateTime =

                today.getFullYear() + '/' +

                (today.getMonth() + 1) + '/' +

                today.getDate() + '/' +

                today.getHours() + ':' + today.getMinutes();
            let url = 'https://localhost:44301/api/test/live';
            chat.client.receiveMessage = function (name, time, message) {
                write_msg(name, time, message);
            };
            $(".btn-primary").click(function () {
                chat.server.send($('#displayname').val(), $('#message').val())
                    .done(function () {
                        fetch(url, {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json'
                            },

                            body: JSON.stringify({
                                ActionType: "D",
                                subAction: "R",
                                Detail: $('#message').val(),
                                Time: currentDateTime,
                                SID: $('#displayname').val(),
                                MID: "M001"
                            })

                        }).then((response) => {
                            return response.json();
                        }).then((jsonData) => {
                            console.log(jsonData);
                        }).catch((err) => {
                            console.log('錯誤:', err);
                        })

                        console.log('send suc');
                        $('#message').val("");
                    })
                    .fail(function (e) {
                        console.warn(e);
                    });
            });
            $.connection.hub.start();
        });
        //function htmlEncode(value) {
        //    var encodedValue = $('<div />').text(value).html();
        //    return encodedValue;
        //}
        function write_msg(name, time, message) {
            var myid = name;
            if (myid == $('#displayname').val()) {
                $('#messages').append('<li class="me"><div><p>我(' + time + ')</p>' + message + '</div><li>');
            }
            else {
                $('#messages').append('<li class="you"><div><p>' + name + '(' + time + ')</p>' + message + '</div><li>');
            }
        }
    </script>
    @*<script>
            $(function () {
                // Reference the auto-generated proxy for the hub.
                var chat = $.connection.chatHub;
                var today = new Date();

                var currentDateTime =

                    today.getFullYear() + '/' +

                    (today.getMonth() + 1) + '/' +

                    today.getDate() + '/' +

                    today.getHours() + ':' + today.getMinutes();
                let url = 'https://localhost:44301/api/test/live';

                // Create a function that the hub can call back to display messages.創建一個函數，集線器可以回調顯示消息
                chat.client.addNewMessageToPage = function (name, message) {

                    // Add the message to the page.將消息添加到頁面
                    $('#discussion').append('<li><strong>' + htmlEncode(name)
                        + '</strong>: ' + htmlEncode(message) + '</li>');
                };
                // Get the user name and store it to prepend to messages.獲取用戶名並將其存儲在消息的前面
                //$('#displayname').val('S001');
                $('#displayname').val(prompt('請輸入暱稱:'));
                // Set initial focus to message input box.將初始焦點設置為消息輸入框
                $('#message').focus();
                // Start the connection.
                $.connection.hub.start().done(function () {
                    $('#sendmessage').click(function () {
                        // Call the Send method on the hub.在集線器上調用 Send 方法
                        chat.server.send($('#displayname').val(), $('#message').val());
                        //fetch(url, {
                        //    method: 'POST',
                        //    headers: {
                        //        'Content-Type': 'application/json'
                        //    },

                        //    body: JSON.stringify({
                        //        ActionType: "D",
                        //        subAction: "R",
                        //        Detail: $('#message').val(),
                        //        Time: currentDateTime,
                        //        SID: "S001",
                        //        MID: "M001"
                        //        //SID: Model.SID,
                        //    })

                        //}).then((response) => {
                        //    return response.json();
                        //}).then((jsonData) => {
                        //    console.log(jsonData);
                        //}).catch((err) => {
                        //    console.log('錯誤:', err);
                        //})
                        // Clear text box and reset focus for next comment.清除文本框並重置下一條評論的焦點
                        $('#message').val('').focus();
                    });
                });
            });
            // This optional function html-encodes messages for display in the page.此可選函數對要在頁面中顯示的消息進行 html 編碼
            function htmlEncode(value) {
                var encodedValue = $('<div />').text(value).html();
                return encodedValue;
            }

        </script>*@
}
