<!-- UploadModal -->
<div class="modal fade" id="UploadModal" tabindex="-1" role="dialog" aria-labelledby="UploadModalLabel" aria-hidden="true">
    <form id="UploadForm" action="@Url.Action("Upload", "Student")" method="post" enctype="multipart/form-data">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="UploadModalLabel">上傳並匯入資料</h5>
                    <button type="button" class="btn-close" aria-hidden="true" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">

                    <div class="file-loading">
                        <input id="file" name="file" type="file" accept=".csv, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet, application/vnd.ms-excel">
                    </div>
                    @*<input id="CID" name="CID" type="hidden" value="@ViewBag.CID" />*@
                <a href="~/FileDownload/Example.xlsx" download>下載範例檔</a>
                </div>
                
                @*<div class="modal-footer">
            <input type="button" id="ButtonCancel" class="btn btn-secondary" data-dismiss="modal" value="取消">
            <input type="submit" id="ButtonSubmit" name="ButtonSubmit" class="btn btn-primary" value="上傳檔案">
        </div>*@
            </div>
        </div>
    </form>
</div>

